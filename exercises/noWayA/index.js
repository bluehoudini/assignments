var newString = "";

let removeA = function(str){
    
}
var newString = "hAckensack"
var 
function findTheA(){
    console.log(newString.indexOf(a))
}

// console.log(newString.charAt(6))
//create an empty string to splice letters into
//create a function that finds the charAt
//if the char is a or A then slice them into newString
//else keep looking
