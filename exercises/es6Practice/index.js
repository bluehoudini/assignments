// const

var pi = 3.141549;

pi = 3;

const phi = 2.1286;

// let is a black-scoped variable declaration

var x = 10;

function funcScope(){
    var x = 11;
}
// template literals
console.log(`${e}`)

// spread operator


// deconstruction/ destructuring

// default parameters
const name = 'John'
let age = 101
let pets = ["cat", "dog"]

const petObjects = []

for (let i = 0; pets.length; i++){
    let pet = {type: pets[i]}
    if (pets[i] === "cat"){
        let name = "fluffy"
    } else {
        let name = "spot"
    }
    pet.name = name
    petObjects.push(pet)
    }

    const vegetables = carrots.map (carrot => type, name)

    friends = people.filter(person => !!person.friendly)

  


  // (a, b) => {a + b};
  const dogs = animals.filter(animal => animal.type === "dog")
  //   if (animal.type === "dog"){
  //     return true
  //   } else {
  //     return false
  //   }
  // })
  // (a, b) => {a + b};


  
    